### Task ID: UA1055
### Task Title: Find Length of List

-----

### 📚 Quick Lesson

**Concept:**
You can find the length of a list using the `len()` function. Example:
```python
arr = [2, 4, 6, 8]
print(len(arr))  # prints 4
```

-----

### 🖥️ Main Statement

**Story:**
Write a program that reads a list of integers from input (space-separated), and prints the length of the list.

**Input:**
A single line containing space-separated integers.

**Output:**
Print the length of the list.

**Sample Tests:**
Input:
```
1 2 3 4 5
```
Output:
```
5
```

Input:
```
10 20 30
```
Output:
```
3
```

**Starter Code**
Copy the code and edit it if you need.

```python
import time
# Start the timer (for debug only)
start_time = time.time()
####START YOUR WORK HERE####

# TODO: read a list and print its length

####END YOUR WORK HERE####
# Stop the timer and print duration (for debug only)
end_time = time.time()
# print(f"Debug: Time taken: {end_time - start_time} seconds")
```
