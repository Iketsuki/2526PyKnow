### Task ID: UA1006
### Task Title: Absolute Value

-----

### 📚 Quick Lesson

**Concept:**
The `abs()` function returns the absolute value of a number. This means it always gives a non-negative result, even if the input is negative.

**Example:**
```python
number = -7
print(abs(number))  # Shows: 7
```

-----

### 🖥️ Main Statement

**Story:**
Read an integer from input. Print its absolute value using the `abs()` function.

**Input:**
The first line contains an integer $N$.

**Output:**
Print $|N|$ (the absolute value of $N$) on a single line.

**Constraints:**
$-100 \leq N \leq 100$

## **Sample Tests:** 
| Input | Output | 
|---|---|
| -7 | 7 |
| 5 | 5 |
| 0 | 0 |

**Starter Code**
Copy the code and edit it if you need.

```python
import time
# Start the timer (for debug only)
start_time = time.time()
####START YOUR WORK HERE####

# Step 1: Read an integer from input and store it in variable N
N = # TODO: fill in the code to read an integer

# Step 2: Calculate the absolute value and store in variable result
result = # TODO: fill in the code to get absolute value

# Step 3: Print the result
print(result)

####END YOUR WORK HERE####
# Stop the timer and print duration (for debug only)
end_time = time.time()
# print(f"Debug: Time taken: {end_time - start_time} seconds")
```
