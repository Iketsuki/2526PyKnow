### Task ID: UA1019
### Task Title: Is it Positive?

-----

### 📚 Quick Lesson

**Concept:**
The `if` statement lets you run code only when a condition is true. For example:

```python
if x > 0:
    print("x is positive")
```

-----

### 🖥️ Main Statement

**Story:**
Given an integer, print "Positive" if it is greater than 0.

**Input:**
The first line contains an integer $n$ ($-1000 \leq n \leq 1000$).

**Output:**
If $n > 0$, print `Positive`.

**Constraints:**
- $-1000 \leq n \leq 1000$

**Sample Tests:**
Input:
```
5
```
Output:
```
Positive
```

Input:
```
0
```
Output:
```
```

**Starter Code**
Copy the code and edit it if you need.

```python
import time
# Start the timer (for debug only)
start_time = time.time()
####START YOUR WORK HERE####

n = int(input())
# TODO: use if statement to check if n > 0

####END YOUR WORK HERE####
# Stop the timer and print duration (for debug only)
end_time = time.time()
# print(f"Debug: Time taken: {end_time - start_time} seconds")
```
