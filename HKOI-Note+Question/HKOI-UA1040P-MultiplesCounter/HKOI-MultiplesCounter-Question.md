### Task ID: UA1040P
### Task Title: Multiples Counter

-----

### 🖥️ Main Statement

**Story:**
Given two numbers $n$ and $k$, count how many numbers from 1 to $n$ are multiples of $k$.

**Input:**
The first line contains an integer $n$ ($1 \leq n \leq 100$).
The second line contains an integer $k$ ($1 \leq k \leq n$).

**Output:**
Print the count of multiples.

**Constraints:**
- $1 \leq n \leq 100$
- $1 \leq k \leq n$

**Sample Tests:**
Input:
```
10
2
```
Output:
```
5
```

Input:
```
15
3
```
Output:
```
5
```

**Starter Code**
Copy the code and edit it if you need.

```python
import time
# Start the timer (for debug only)
start_time = time.time()
####START YOUR WORK HERE####

n = int(input())
k = int(input())
# TODO: count how many numbers from 1 to n are multiples of k

####END YOUR WORK HERE####
# Stop the timer and print duration (for debug only)
end_time = time.time()
# print(f"Debug: Time taken: {end_time - start_time} seconds")
```
